import { screen, render } from "@testing-library/react";

import AddActivityModal from "../AddActivityModal";

test("Test to ensure modal saves actvivity and closes when add button is closed", async () => {});
